# Copy to ".env" and fill in your Supabase project values.
# Find these in the Supabase dashboard → Project Settings → API.
# Leave BOTH unset to run the app in in-memory demo mode (no backend).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
